#include <gtest/gtest.h>

TEST(Placeholder, ReplaceMe) {
    EXPECT_TRUE(true) << "Replace this with a real test that compares your "
                         "kernel output to a CPU reference and asserts max "
                         "abs error within tolerance.";
}
